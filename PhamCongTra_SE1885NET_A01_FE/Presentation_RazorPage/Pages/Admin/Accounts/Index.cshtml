@page
@model Presentation_RazorPage.Pages.Admin.Accounts.IndexModel
@{
    ViewData["Title"] = "Manage Accounts";
}

<div class="container-fluid">
    <!-- Header Section -->
    <div class="row">
        <div class="col-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/Admin/Dashboard">Dashboard</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Manage Accounts</li>
                </ol>
            </nav>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="h3 mb-0">Manage User Accounts</h1>
                    <p class="text-muted">Create and manage system user accounts</p>
                </div>
                <div>
                    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createAccountModal">
                        <i class="fas fa-user-plus me-2"></i>Create New Account
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Success/Error Messages -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>@TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle me-2"></i>@TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Statistics Cards -->
    <div class="row mb-4">
        <div class="col-md-4 mb-3">
            <div class="card bg-primary text-white shadow-sm">
                <div class="card-body">
                    <div class="row align-items-center">
                        <div class="col">
                            <h6 class="text-uppercase text-white-50 mb-1">Total Accounts</h6>
                            <h3 class="mb-0">@Model.TotalAccounts</h3>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-users fa-2x"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-3">
            <div class="card bg-success text-white shadow-sm">
                <div class="card-body">
                    <div class="row align-items-center">
                        <div class="col">
                            <h6 class="text-uppercase text-white-50 mb-1">Staff Accounts</h6>
                            <h3 class="mb-0">@Model.StaffAccounts</h3>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-user-tie fa-2x"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-3">
            <div class="card bg-info text-white shadow-sm">
                <div class="card-body">
                    <div class="row align-items-center">
                        <div class="col">
                            <h6 class="text-uppercase text-white-50 mb-1">Lecturer Accounts</h6>
                            <h3 class="mb-0">@Model.LecturerAccounts</h3>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-chalkboard-teacher fa-2x"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Search and Filter -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form method="get" class="row g-3">
                        <div class="col-md-6">
                            <label for="searchTerm" class="form-label">Search Accounts</label>
                            <input type="text" class="form-control" id="searchTerm" name="searchTerm"
                                   value="@Model.SearchTerm" placeholder="Search by name or email...">
                        </div>
                        <div class="col-md-4">
                            <label for="roleFilter" class="form-label">Filter by Role</label>
                            <select class="form-select" id="roleFilter" name="roleFilter">
                                <option value="">All Roles</option>
                                <option value="1" selected="@(Model.RoleFilter == 1)">Staff</option>
                                <option value="2" selected="@(Model.RoleFilter == 2)">Lecturer</option>
                            </select>
                        </div>
                        <div class="col-md-2 d-flex align-items-end">
                            <button type="submit" class="btn btn-primary w-100">
                                <i class="fas fa-search me-2"></i>Search
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Accounts Table -->
    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h6 class="mb-0 fw-semibold text-primary">User Accounts (@Model.Accounts.Count)</h6>
                </div>
                <div class="card-body">
                    @if (Model.Accounts.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Name</th>
                                        <th>Email</th>
                                        <th>Role</th>
                                        <th>Articles</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var account in Model.Accounts.OrderBy(a => a.AccountId))
                                    {
                                        <tr>
                                            <td>
                                                <strong>@account.AccountId</strong>
                                            </td>
                                            <td>
                                                <div>
                                                    <strong>@account.AccountName</strong>
                                                </div>
                                            </td>
                                            <td>
                                                <span class="text-muted">@account.AccountEmail</span>
                                            </td>
                                            <td>
                                                @switch (account.AccountRole)
                                                {
                                                    case 1:
                                                        <span class="badge bg-primary">
                                                            <i class="fas fa-user-tie me-1"></i>Staff
                                                        </span>
                                                        break;
                                                    case 2:
                                                        <span class="badge bg-info">
                                                            <i class="fas fa-chalkboard-teacher me-1"></i>Lecturer
                                                        </span>
                                                        break;
                                                    default:
                                                        <span class="badge bg-danger">
                                                            <i class="fas fa-user-shield me-1"></i>Admin
                                                        </span>
                                                        break;
                                                }
                                            </td>
                                            <td>
                                                <span class="badge bg-secondary">@account.ArticleCount</span>
                                            </td>
                                            <td>
                                                <div class="btn-group" role="group">
                                                    <button type="button" class="btn btn-sm btn-outline-primary"
                                                            onclick="editAccount(@account.AccountId, '@account.AccountName', '@account.AccountEmail', @account.AccountRole)"
                                                            data-bs-toggle="modal" data-bs-target="#editAccountModal" title="Edit">
                                                        <i class="fas fa-edit"></i>
                                                    </button>

                                                    @if (account.ArticleCount == 0)
                                                    {
                                                        <form method="post" class="d-inline" asp-page-handler="Delete"
                                                              asp-route-id="@account.AccountId">
                                                            <button type="submit" class="btn btn-sm btn-outline-danger"
                                                                    title="Delete"
                                                                    onclick="return confirm('Are you sure you want to delete this account?')">
                                                                <i class="fas fa-trash"></i>
                                                            </button>
                                                        </form>
                                                    }
                                                    else
                                                    {
                                                        <button type="button" class="btn btn-sm btn-outline-secondary"
                                                                title="Cannot delete - has articles" disabled>
                                                            <i class="fas fa-ban"></i>
                                                        </button>
                                                    }
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <i class="fas fa-users fa-3x text-muted mb-3"></i>
                            <h5 class="text-muted">No Accounts Found</h5>
                            <p class="text-muted">
                                @if (!string.IsNullOrEmpty(Model.SearchTerm) || Model.RoleFilter.HasValue)
                                {
                                    <span>No accounts match your search criteria.</span>
                                }
                                else
                                {
                                    <span>No user accounts have been created yet.</span>
                                }
                            </p>
                            <div class="mt-3">
                                @if (string.IsNullOrEmpty(Model.SearchTerm) && !Model.RoleFilter.HasValue)
                                {
                                    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createAccountModal">
                                        <i class="fas fa-user-plus me-2"></i>Create First Account
                                    </button>
                                }
                                else
                                {
                                    <a href="/Admin/Accounts" class="btn btn-primary">
                                        <i class="fas fa-refresh me-2"></i>Clear Filters
                                    </a>
                                }
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Create Account Modal -->
<div class="modal fade" id="createAccountModal" tabindex="-1" aria-labelledby="createAccountModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="Create">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title" id="createAccountModalLabel">Create New Account</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                    <div class="mb-3">
                        <label asp-for="CreateAccount.AccountName" class="form-label">Full Name *</label>
                        <input asp-for="CreateAccount.AccountName" class="form-control" />
                        <span asp-validation-for="CreateAccount.AccountName" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="CreateAccount.AccountEmail" class="form-label">Email Address *</label>
                        <input asp-for="CreateAccount.AccountEmail" type="email" class="form-control" />
                        <span asp-validation-for="CreateAccount.AccountEmail" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="CreateAccount.AccountPassword" class="form-label">Password *</label>
                        <input asp-for="CreateAccount.AccountPassword" type="password" class="form-control" />
                        <span asp-validation-for="CreateAccount.AccountPassword" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="CreateAccount.AccountRole" class="form-label">Role *</label>
                        <select asp-for="CreateAccount.AccountRole" class="form-select">
                            <option value="">Select Role</option>
                            <option value="1">Staff</option>
                            <option value="2">Lecturer</option>
                        </select>
                        <span asp-validation-for="CreateAccount.AccountRole" class="text-danger"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">Create Account</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Account Modal -->
<div class="modal fade" id="editAccountModal" tabindex="-1" aria-labelledby="editAccountModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form id="editAccountForm" method="post" asp-page-handler="Update">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title" id="editAccountModalLabel">Edit Account</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="editAccountId" name="AccountId" />

                    <div class="mb-3">
                        <label class="form-label">Full Name *</label>
                        <input id="editAccountName" name="AccountName" class="form-control" required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Email Address *</label>
                        <input id="editAccountEmail" name="AccountEmail" type="email" class="form-control" required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Role *</label>
                        <select id="editAccountRole" name="AccountRole" class="form-select" required>
                            <option value="1">Staff</option>
                            <option value="2">Lecturer</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function editAccount(id, name, email, role) {
            document.getElementById('editAccountId').value = id;
            document.getElementById('editAccountName').value = name;
            document.getElementById('editAccountEmail').value = email;
            document.getElementById('editAccountRole').value = role;
        }
    </script>
}

